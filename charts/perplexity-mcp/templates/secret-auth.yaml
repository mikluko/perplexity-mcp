{{- if .Values.auth.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "perplexity-mcp.fullname" . }}-auth
  labels:
    {{- include "perplexity-mcp.labels" . | nindent 4 }}
type: Opaque
stringData:
  # Traefik users file format: username:hashed-password
  users: |
    {{- if .Values.auth.htpasswd }}
    {{ .Values.auth.htpasswd }}
    {{- else }}
    {{ htpasswd .Values.auth.username .Values.auth.password }}
    {{- end }}
{{- end }}
