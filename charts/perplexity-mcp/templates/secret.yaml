{{- if not .Values.perplexity.existingSecret }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "perplexity-mcp.fullname" . }}
  labels:
    {{- include "perplexity-mcp.labels" . | nindent 4 }}
type: Opaque
data:
  api-key: {{ .Values.perplexity.apiKey | b64enc | quote }}
{{- end }}
